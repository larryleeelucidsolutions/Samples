Scopus: Import CSV Readme
-------------------------

The Scopus Import CSV module defines an API that can be used to import
Scopus records from CSV export files that have been generated by
Scopus.

The module also defines a Drush command that can be used to import
records from these files.

Components
----------

This module defines the following content types, vocabularies, views,
blocks, roles, etc: 

  Drush Commands:
    scopus-import-csv-file
      Imports the Scopus records listed in the given CSV file. Note:
      the file must use the "Abstract Format" export format.
