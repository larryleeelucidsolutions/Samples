Google Scholar Import Readme
============================

The Google Scholar Import module defines an API that can be used to
import records from Google Scholar. The module defines a Cron job that
can be configured to automatically import records from Google Scholar.

The module defines the following components:

  * Cron Jobs
    google_scholar_cron
    Imports records from Google Scholar that match certain search terms.
  * Pages
    Google Scholar Import Settings
    admin/config/google_scholar_import/settings
    The Google Scholar Import settings page.
  
